
## Semantic ownership

CPU owns processor identity and counts in addition to its existing execution capabilities. CPU.ID denotes a processor position; CPU.Count denotes cardinality. Discovery and multi-processor/NUMA topology belong to platform compositions. The existing atomic/cache/checksum capability collection remains queued for semantic reduction.
